# Vercel Environment Variables Example
# Copy these to your Vercel project environment variables

# Database
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/warehouse?retryWrites=true&w=majority

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
NODE_ENV=production
VERCEL=1

# CORS (comma-separated origins)
# For local development:
CORS_ORIGIN=http://localhost:3000,http://localhost:5000,http://127.0.0.1:3000,http://127.0.0.1:5000
# For production (Vercel):
CORS_ORIGIN=https://cleanjshipping.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# API Keys
API_KEY_SECRET=your-api-key-secret-change-this

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads
