# Vercel Environment Variables Example
# Copy ALL required vars to Vercel → Settings → Environment Variables
# Enable: Production, Preview, AND Development — then Redeploy

# Database (REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/warehouse?retryWrites=true&w=majority

# JWT (REQUIRED — app will crash without this even if MONGODB_URI is set)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration (REQUIRED)
NODE_ENV=production
VERCEL=1
BASE_URL=https://cleanjshipping.vercel.app

# CORS (comma-separated origins)
# For production (Vercel):
CORS_ORIGIN=https://cleanjshipping.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# API Keys
API_KEY_SECRET=your-api-key-secret-change-this

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads
